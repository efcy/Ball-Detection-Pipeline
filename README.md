# Ball Detection Demo

Example Image is from https://vat.berlin-united.com/api/images/12029428/

Run find_annotation_by_labeld_party.py for looking in all labeled data for field border, lines and balls and download fitting images + annotation. Set sample_size in main, otherwise scans all logs. 

Run scanline_with_given_lines.py

For checking parameter for green detection use green-detecor.py (UV-histogram) or adaptive_green_detector.py (scalable values)